name: Test Project
on: workflow_dispatch
jobs:
  unit-test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v4
      # - name: Install NodeJS
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: '18'
      - name: Install dependencies
        run: npm ci
      - name: Run tests
        run: npx playwright test --project=Desktop_Chrome
  deploy:
    needs: [unit-test]
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v4
      - name: Deploy application
        run: echo "Deploying application..."